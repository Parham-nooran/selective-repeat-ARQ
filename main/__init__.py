import threading
import time

from main.logic.client import *
from main.logic.server import *

if __name__ == '__main__':
    print(bytes(32))
    # second = threading.Thread(target=server_program())
    # time.sleep(5)
    # first = threading.Thread(target=client_program())
